IMAGE_NAME=test:latest  # 你的 langgraph build 输出镜像名；若使用官方基础镜像请替换为 langchain/langgraph-api:3.13-wolfi

# Required for production / server start
LANGSMITH_API_KEY=your_langsmith_api_key
LANGSMITH_ENDPOINT=https://api.smith.langchain.com
# LANGGRAPH_CLOUD_LICENSE_KEY=your_license_key  # 有正式 license 时填入 JWT，没有可保持注释，运行 Lite 模式

# Redis and Postgres defaults match docker-compose.langgraph.yml
REDIS_URI=redis://langgraph-redis:6379
DATABASE_URI=postgres://postgres:postgres@langgraph-postgres:5432/postgres?sslmode=disable

# Optional: point to self-hosted LangSmith for traces
# LANGSMITH_ENDPOINT=https://your-langsmith-host
