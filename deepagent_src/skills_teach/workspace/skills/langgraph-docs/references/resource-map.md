# Resource Map

RESOURCE_REFERENCE_MARKER

Use this reference when you need to explain how this skill's supporting files are organized.

- `references/` contains detailed reading material.
- `scripts/` contains executable helpers.
- `assets/` contains reusable templates or static files.
