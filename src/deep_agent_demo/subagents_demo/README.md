# Subagents Demo

本目录学习要点（文件夹内先列出学习的内容）：
- 通过 `subagents=[...]` 为主 Agent 配置专用子智能体，主智能体用 `task()` 工具委派
- 适用场景：研究/整理/生成等多步骤任务的上下文隔离与专业化处理
- 子智能体可以各自拥有模型、工具、系统提示，避免主上下文被大输出淹没
- `general-purpose` 子智能体随主 Agent 自带，用于通用隔离
- 示例代码：`agent.py` 展示两个字典式子智能体（研究、写手）与统一的对话打印入口
